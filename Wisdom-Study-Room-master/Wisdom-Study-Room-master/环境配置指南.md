# 🛠️ 环境配置指南（macOS / Windows）

本指南面向在 macOS 和 Windows 上准备并运行 Wisdom-Study-Room 项目的开发者。文中所有路径以“项目根目录”表示（即包含 `pom.xml`、`init-database.sql` 的目录），请在自己的机器上替换为实际路径。

---

## 系统需求（总结）

- Java 17（建议与项目 POM 保持一致）
- Maven（可选：可使用 Maven Wrapper `mvnw`）
- MySQL 8.x（或兼容的 MariaDB）
- Node.js（建议使用 nvm 管理，Node 18/20 均可）
- 推荐安装 Docker（可选，用于数据库或容器化部署）

---

## macOS 安装与配置（推荐使用 Homebrew）

1) 安装 Homebrew（如果尚未安装）

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

2) 安装 OpenJDK 17、Maven、MySQL、和 Node

```bash
brew update
brew install openjdk@17 maven mysql
# 若想同时安装 node，可使用 nvm（见下）或直接 brew install node
```

3) 将 openjdk@17 添加到系统（Homebrew 会在安装后提示具体命令）

```bash
sudo ln -sfn /opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk-17.jdk
```

4) 启动并配置 MySQL

```bash
brew services start mysql
mysql_secure_installation
```

5) 使用 nvm 管理 Node.js（可选但推荐）

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm install --lts
nvm use --lts
```

6) 导入数据库初始化脚本（在项目根执行）

```bash
# 可先创建项目用户（可选）
mysql -u root -p -e "CREATE USER IF NOT EXISTS 'wisdom'@'localhost' IDENTIFIED BY 'wisdom_study_room'; GRANT ALL PRIVILEGES ON wisdom_study_room.* TO 'wisdom'@'localhost'; FLUSH PRIVILEGES;"

# 导入初始化 SQL
mysql -u wisdom -pwisdom_study_room < ./init-database.sql
```

---

## Windows 安装参考（简要）

可使用 Chocolatey（需以管理员身份运行 PowerShell）或手动下载安装 Java、Maven、Node 与 MySQL。示例（使用 Chocolatey）：

```powershell
# 安装 Chocolatey（管理员 PowerShell）
Set-ExecutionPolicy Bypass -Scope Process -Force; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# 安装工具
choco install maven -y
choco install nodejs-lts -y
choco install mysql -y
# 刷新环境
refreshenv
```

---

## Maven Wrapper（跨平台）

如果项目包含 Maven Wrapper（`mvnw` / `mvnw.cmd`），则无需全局安装 Maven：

```bash
./mvnw clean package -DskipTests   # macOS / Linux
./mvnw spring-boot:run

# Windows
.\mvnw.cmd clean package -DskipTests
.\mvnw.cmd spring-boot:run
```

检查项目根是否存在 Wrapper：

```bash
ls -la mvnw* || true
```

---

## 启动项目（开发模式）

后端（在项目根）：

```bash
# 使用本机 Maven
mvn spring-boot:run
# 或使用 Wrapper
./mvnw spring-boot:run
```

前端（在 `vue_project` 目录）：

```bash
cd vue_project
npm install
npm run serve
# 或用 npx 启动（无需全局安装 @vue/cli）

```

---

## 安装后验证

```bash
# Java
java -version
# Maven
mvn -version
# Node / npm
node -v
npm -v
# MySQL
mysql --version
# 数据库连接测试
mysql -u wisdom -pwisdom_study_room -e "SHOW TABLES IN wisdom_study_room;"
```

---

## 常见问题与排查（简要）

- 端口占用：后端端口（默认 8080）或前端端口冲突，请修改 `application.properties` 中的 `server.port` 或 `vue.config.js` 的 `port`。
- MySQL 认证问题：MySQL 8 默认使用 `caching_sha2_password`，如有 JDBC 兼容问题，可把用户认证改为 `mysql_native_password`：

```sql
ALTER USER 'wisdom'@'localhost' IDENTIFIED WITH mysql_native_password BY 'wisdom_study_room';
FLUSH PRIVILEGES;
```

- npm 依赖冲突：若遇 peer dependency 报错，使用 `npm install --legacy-peer-deps` 或切换到与项目兼容的 Node 版本（nvm 管理）。

---

## 初始化完成

按以上步骤在你的操作系统上完成安装与数据库导入后，参考 `运行指南.md` 启动并访问应用。

**测试账号（示例）**
- 管理员: admin / admin123
- 普通用户: testuser / user123
```
