# 🎯 智慧自习室 - 快速启动手册

## 📋 当前状态

### 环境检测结果
- ✅ **Java 21**: 已安装
- ❌ **Maven**: 未检测到 (需要安装)
- ❌ **Node.js**: 未检测到 (需要安装)
- ❌ **MySQL**: 未检测到 (需要安装)

---

## 🚀 三步启动法

### 第一步: 安装必要软件 ⚙️

您需要安装以下软件,请选择一种方式:

#### 方式 A: 一键安装 (推荐)

1. **以管理员身份打开 PowerShell**
2. **安装 Chocolatey** (Windows 包管理器):
   ```powershell
   Set-ExecutionPolicy Bypass -Scope Process -Force; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
   ```
3. **安装所有依赖**:
   ```powershell
   choco install maven nodejs-lts mysql -y
   ```
4. **重启 PowerShell**

#### 方式 B: 手动下载安装

**Maven:**
- 下载: https://maven.apache.org/download.cgi
- 解压到: `C:\Program Files\Apache\maven`
- 添加环境变量: `MAVEN_HOME` 和 `Path`

**Node.js:**
- 下载: https://nodejs.org/ (选择 LTS 版本)
- 运行安装程序,勾选 "Add to PATH"

**MySQL:**
- 下载: https://dev.mysql.com/downloads/mysql/
- 或使用 XAMPP: https://www.apachefriends.org/

**详细步骤**: 请查看 `环境配置指南.md`

---

### 第二步: 初始化数据库 🗄️

#### 2.1 启动 MySQL 服务

**如果安装了 MySQL Server:**
```powershell
# 以管理员身份运行
net start mysql
```

**如果使用 XAMPP:**
- 打开 XAMPP Control Panel
- 启动 MySQL

#### 2.2 创建数据库和用户

打开 PowerShell,执行:
```powershell
# 登录 MySQL (默认密码可能为空或您安装时设置的密码)
mysql -u root -p
```

在 MySQL 中执行:
```sql
-- 创建用户
CREATE USER 'wisdom'@'localhost' IDENTIFIED BY 'wisdom_study_room';

-- 授权
GRANT ALL PRIVILEGES ON wisdom_study_room.* TO 'wisdom'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 2.3 导入数据库结构和测试数据

```powershell
# 进入项目目录
cd "c:\Users\czy-0\OneDrive - czy0412\Lessons\软件工程设计\Wisdom-Study-Room-master\Wisdom-Study-Room-master"

# 执行 SQL 脚本
mysql -u wisdom -pwisdom_study_room < init-database.sql
```

成功后会显示数据统计。

---

### 第三步: 启动应用 🎉

#### 3.1 启动后端服务

**打开第一个 PowerShell 窗口:**
```powershell
# 进入项目目录
cd "c:\Users\czy-0\OneDrive - czy0412\Lessons\软件工程设计\Wisdom-Study-Room-master\Wisdom-Study-Room-master"

# 方式1: 使用批处理脚本 (推荐)
.\启动后端.bat

# 方式2: 使用 Maven 命令
mvn clean spring-boot:run
```

看到以下信息表示启动成功:
```
Started DemoApplication in X.XXX seconds
```

后端服务地址: **http://localhost:8080**

#### 3.2 启动前端服务

**打开第二个 PowerShell 窗口:**
```powershell
# 进入前端目录
cd "c:\Users\czy-0\OneDrive - czy0412\Lessons\软件工程设计\Wisdom-Study-Room-master\Wisdom-Study-Room-master\vue_project"

# 方式1: 使用批处理脚本 (推荐)
.\启动前端.bat

# 方式2: 使用 npm 命令
npm install  # 首次运行需要安装依赖
npm run serve
```

看到以下信息表示启动成功:
```
App running at:
- Local:   http://localhost:8081/
- Network: http://192.168.x.x:8081/
```

前端访问地址: **http://localhost:8081**

---

## 🎮 开始使用

### 1. 打开浏览器访问
```
http://localhost:8081
```

### 2. 使用测试账号登录

#### 管理员账号
- **账号**: `admin`
- **密码**: `admin123`
- **功能**: 可以管理所有数据

#### 普通用户账号
- **账号**: `testuser`
- **密码**: `user123`
- **功能**: 可以预约座位、签到签退

### 3. 或者注册新账号
点击"注册"按钮创建自己的账号

---

## 🛠️ 故障排除

### 问题 1: 后端启动失败

#### 错误: "Access denied for user 'wisdom'@'localhost'"
**原因**: 数据库用户未创建或密码错误  
**解决**: 重新执行第二步的数据库初始化

#### 错误: "Port 8080 is already in use"
**原因**: 端口被占用  
**解决**: 
```powershell
# 查找占用端口的进程
netstat -ano | findstr :8080

# 结束进程 (PID 是上一步查到的进程 ID)
taskkill /F /PID <PID>
```
或修改 `src\main\resources\application.properties`:
```properties
server.port=8081
```

#### 错误: "Table 'wisdom_study_room.user' doesn't exist"
**原因**: 数据库未初始化  
**解决**: 执行数据库初始化脚本

---

### 问题 2: 前端启动失败

#### 错误: "npm: command not found"
**原因**: Node.js 未安装或未配置环境变量  
**解决**: 重新安装 Node.js 并确保勾选 "Add to PATH"

#### 错误: npm 安装依赖很慢或失败
**解决**: 使用国内镜像
```powershell
npm config set registry https://registry.npmmirror.com
npm install
```

#### 错误: "Port 8081 is already in use"
**解决**: 修改 `vue.config.js` 中的端口号:
```javascript
devServer: {
  port: 8082,  // 改成其他端口
  // ...
}
```

---

### 问题 3: 前后端通信失败

#### 浏览器控制台显示 404 或网络错误
**检查**:
1. 后端是否启动成功 (访问 http://localhost:8080)
2. 前端代理配置是否正确 (vue.config.js)
3. 浏览器按 F12 查看 Network 标签的请求详情

**解决**: 确保 `vue.config.js` 配置正确:
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',  // 确保指向本地后端
    changeOrigin: true,
    pathRewrite: {
      '^/api': ''
    }
  }
}
```

---

## 📁 重要文件说明

```
项目根目录/
├── init-database.sql           # 数据库初始化脚本
├── 启动后端.bat                # 后端启动脚本
├── 运行指南.md                 # 详细运行指南
├── 环境配置指南.md             # 环境安装指南
├── pom.xml                     # Maven 配置
├── src/
│   └── main/
│       └── resources/
│           └── application.properties  # 后端配置文件
└── vue_project/
    ├── 启动前端.bat            # 前端启动脚本
    ├── vue.config.js           # Vue 配置文件
    └── package.json            # npm 依赖配置
```

---

## 🔄 停止服务

### 停止后端
在后端 PowerShell 窗口按 `Ctrl + C`

### 停止前端
在前端 PowerShell 窗口按 `Ctrl + C`

### 停止 MySQL (可选)
```powershell
# 以管理员身份运行
net stop mysql
```

---

## 📞 获取帮助

如果遇到问题:

1. **查看错误日志**: 仔细阅读终端输出的错误信息
2. **检查配置文件**: 确认数据库连接、端口等配置正确
3. **查看文档**: 
   - `环境配置指南.md` - 环境安装问题
   - `运行指南.md` - 详细运行说明
4. **浏览器开发者工具**: 按 F12 查看网络请求和控制台错误

---

## 🎯 下一步

系统启动成功后,您可以:

1. ✅ 熟悉系统功能
2. ✅ 查看源代码了解实现
3. ✅ 尝试添加新功能
4. ✅ 修改样式和界面
5. ✅ 编写测试用例

**祝您使用愉快! 🎉**
